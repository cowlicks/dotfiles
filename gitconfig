[user]
    name = Blake Griffith
    email = blake.a.griffith@gmail.com

[color]
    diff = auto
    status = auto
    branch = auto
    ui = auto

[core]
    editor = vim
	excludesfile = /home/blake/.global_gitignore

[merge]
    summary = true
[push]
	default = simple
[alias]
	pac = "!f(){ git add --patch \"${GIT_PREFIX}./$1\" && git commit; }; f"
	freebase = "!f(){ git fetch origin && git rebase origin/master; }; f"
